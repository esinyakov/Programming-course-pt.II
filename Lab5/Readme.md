Используем уже разработанный класс Point. Создаем массив указателей на функции, добавим туда
указатели на sin(x) и на cos(x). Разработать класс Baza_tab, который включает
метод табуляции, он принимает на вход указатель на функцию и по этой функции строит массив точек.
Так же этот класс включает метод который возрвращает интервалы нулей, от обработанной функции.
Создать объекты этого класса и вызвать соответсвтующие методы.